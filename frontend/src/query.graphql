  query GetCakes($glutenFree: Boolean!) {
    cake(filters: { glutenFree: { eq: $glutenFree } }) {
      nodes {
        id
        name
        price
        glutenFree
        bakery {
          name
        }
      }
    }
  }
